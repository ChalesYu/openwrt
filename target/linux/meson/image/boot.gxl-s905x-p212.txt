setenv bootargs "root=/dev/mmcblk0p2 rootwait rw rootfstype=ext4,squashfs console=tty0, console=ttyAML0,115200 net.ifnames=0"
setenv loadaddr "0x01080000"
setenv dtb_loadaddr "0x01000000"

fatload mmc 0 ${loadaddr} uImage
fatload mmc 0 ${dtb_loadaddr} meson-gxl-s905x-p212.dtb

bootm ${loadaddr} - ${dtb_loadaddr}
